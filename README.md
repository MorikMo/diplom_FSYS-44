# 📘 README — Дипломный проект: отказоустойчивая инфраструктура в Yandex Cloud

## 📌 Описание проекта
Цель проекта — разработать отказоустойчивую, безопасную и масштабируемую инфраструктуру для веб-приложения.  
Инфраструктура разворачивается в Yandex Cloud с использованием Terraform и Ansible и включает:

- 2 web-сервера (nginx)
- Application Load Balancer
- Bastion host
- Zabbix Server + агенты
- Elasticsearch + Kibana
- Filebeat для сбора логов
- NAT-gateway
- Security Groups
- Snapshot backup (ежедневный)

## 🗂 Структура репозитория
terraform/, ansible/, README.md

## ⚙ Terraform
Разворачивает всю инфраструктуру: VPC, NAT, web-сервера, bastion, Zabbix, Kibana, Elasticsearch, ALB.

Команды:
terraform init  
terraform plan  
terraform apply

## 🔐 Безопасность
- Bastion — единственный публичный SSH.
- Web — только от ALB и Zabbix.
- Kibana — ограничена.
- Elasticsearch — приватная.

## 🌐 Балансировщик
Target group → backend group → http router → ALB  
Проверка: curl -v http://<ALB_IP>

## 📊 Мониторинг
Zabbix server + agent2 на всех ВМ.  
USE-дэшборды.

## 📜 Логи
Elasticsearch (priv), Kibana (pub).  
Filebeat на web1/web2 → отправляет access.log и error.log.

## 💾 Snapshot Backup
Ежедневно в 03:00 UTC, хранение 7 дней.  
Привязка дисков выполнена вручную (Terraform не поддерживает).

## 🧪 Проверки
curl -v http://<alb_ip>  
http://<kibana_ip>:5601
